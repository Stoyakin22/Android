fun main() {
    var number = 1
    for (i in 1..11) { // Adjust the upper bound (11) to control the number of elements
        print("$number ")
        number *= 2
    }
    println() // Add a newline at the end for better formatting.
}
